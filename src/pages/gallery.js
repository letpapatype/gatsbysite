import React from 'react'

// calling the frame component in. <h1>This is the page tile</>
import Frame from '../components/frame'

const GalleryPage = () => {
    return (
        <Frame>
            <p>This will be in as children</p>
        </Frame>
    )
}

export default GalleryPage 